# Общий слой (Shared Layer)

В этой папке располагается код, используемый несколькими или всеми ботами.

- `db/` — модели базы данных (ORM) и сессии подключения.
- `events/` — протоколы обмена событиями между ботами.
- `utils/` — валидаторы, форматирование, логирование.
